You are an expert software architect. Analyze the user's request and create a comprehensive project specification.

User Query: {user_query}

CRITICAL: You MUST return ONLY valid JSON. No markdown, no explanations, JUST the JSON object.

Your task is to generate a JSON object with this EXACT structure:

{{
  "project_name": "descriptive_project_name",
  "description": "Clear description of what this project does",
  "files": [
    {{
      "path": "main.py",
      "content": "",
      "file_type": "python",
      "description": "Main entry point that orchestrates the workflow"
    }}
  ],
  "dependencies": [
    {{
      "package": "langchain",
      "version": "0.1.0",
      "purpose": "LLM orchestration"
    }}
  ],
  "env_variables": {{
    "API_KEY": "API key description"
  }},
  "entry_point": "main.py",
  "test_cases": [
    "Test basic workflow execution"
  ],
  "readme_content": "# Project Title\\n\\nDescription and usage instructions"
}}

JSON Validation Checklist:
- All strings must use double quotes ("), not single quotes (')
- All object keys must be in double quotes
- All arrays must end with ] not ,]
- All objects must end with }} not ,}}
- Use \\n for newlines in strings, not actual line breaks
- No trailing commas anywhere
- No comments in JSON

Guidelines:
1. Include 3-5 files minimum for a working project
2. Specify clear file descriptions
3. List all required dependencies
4. Include environment variables needed
5. Add comprehensive README content

RETURN ONLY THE JSON OBJECT. START WITH {{ and END WITH }}. NO OTHER TEXT.